// Main library entry point for Unstructured DS

// Export all components
export * from './components'

// Note: Icons are exported separately via './icons' path
// This keeps the main bundle small and allows tree-shaking
// Usage: import { IconArchive } from 'unstructured-ds/icons'

// Export design tokens
export * from './build/tokens'

// Export utilities
export * from './utils'

